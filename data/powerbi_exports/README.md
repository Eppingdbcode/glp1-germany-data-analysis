# Power BI public exports

These model-ready tables are copied deterministically from `data/processed/` by notebook 07. They contain only redistributable public-source data.

WIdO observations are intentionally absent because redistribution rights have not been confirmed. The GLP-1/WIdO results retained in the executed notebook, analytical figures and dashboard screenshots require authorized external data for full reproduction; see `data/README.md`.
